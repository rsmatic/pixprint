{
  "name": "pixprint",
  "version": "1.0.0",
  "private": true,
  "description": "Printer repair & maintenance management",
  "scripts": {
    "setup": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "seed:demo": "node server/src/seed-demo.js"
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
